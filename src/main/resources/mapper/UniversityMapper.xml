<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.choosephd.repository.UniversityMapper">

    <sql id="baseColumns">
        u.url_id, u.name_zh, u.name_en, u.name_zh_tw, u.country, u.region,
        u.badge_url, u.website, u.motto, u.founded_date, u.address,
        u.created_at, u.updated_at, u.deleted
    </sql>

    <select id="selectSearchPage" resultType="com.choosephd.dto.UniversityVo">
        SELECT DISTINCT
            <include refid="baseColumns"/>,
            best.best_rank_value
        FROM university u
        LEFT JOIN (
            SELECT re.university_id, MIN(re.rank_value) AS best_rank_value
            FROM ranking_entry re
            WHERE re.deleted = 0
              AND re.subject_id IS NULL
              AND re.rank_value IS NOT NULL
              AND re.rank_value &lt; 9999
              AND re.source_id IN (4, 11, 16, 19, 46, 54, 268)
            GROUP BY re.university_id
        ) best ON u.url_id = best.university_id
        <if test="tagIds != null and !tagIds.isEmpty()">
            <foreach collection="tagIds" item="tid" index="idx">
                JOIN university_tag_relation utr${idx} ON utr${idx}.university_id = u.url_id AND utr${idx}.tag_id = #{tid}
            </foreach>
        </if>
        WHERE u.deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (
                u.url_id = #{keyword}
                OR u.name_zh LIKE CONCAT('%', #{keyword}, '%')
                OR u.name_en LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="country != null and country != ''">
            AND u.country = #{country}
        </if>
        <!-- continent 兼容 dirty data: 历史数据有把 continent 名 (如 '亚洲') 误填到 country 列的情况.
             这里保留 OR u.country = #{continent}, 即使前端传的是 continent 名, 也能匹配脏数据行.
             同时 OR u.country IN (continentCountries) 匹配正经 country.
             脏数据规模 < 50 行, 暂时不修, SQL 加注释避免后人误删. -->
        <if test="continent != null and continent != ''">
            AND (
                u.country = #{continent}
                <if test="continentCountries != null and !continentCountries.isEmpty()">
                    OR u.country IN
                    <foreach collection="continentCountries" item="c" open="(" separator="," close=")">
                        #{c}
                    </foreach>
                </if>
            )
        </if>
        <choose>
            <when test="sortBy == 'name'">
                ORDER BY u.name_en ASC
            </when>
            <otherwise>
                ORDER BY
                    CASE WHEN best.best_rank_value IS NULL THEN 1 ELSE 0 END ASC,
                    best.best_rank_value ASC,
                    u.name_en ASC
            </otherwise>
        </choose>
    </select>

    <select id="selectCountries" resultType="java.lang.String">
        SELECT DISTINCT u.country
        FROM university u
        WHERE u.deleted = 0
          AND u.country IS NOT NULL
          AND u.country != ''
          AND u.country != 'unknown'
          <!-- 过滤掉 continent 名 + 中国省份名 + 院校类型名三类脏数据, 让 SELECT DISTINCT country
               返干净的"国家级"列表. 脏数据规模 < 50 行 (临时保留 OR 兼容, 等下一次批量清洗时
               跟前端 country 选择器改造成 continent 分组后再删). -->
          AND u.country NOT IN ('亚洲', '欧洲', '北美洲', '非洲', '南美洲', '大洋洲',
                                '综合类', '师范类', '理工类', '政法类', '艺术类',
                                '医药类', '财经类', '体育类', '农林类', '民族类',
                                '语言类', '军事类')
          AND u.country NOT REGEXP '^(山东|山西|江苏|河南|新疆|河北|湖北|湖南|广东|广西|海南|四川|贵州|云南|西藏|陕西|甘肃|青海|辽宁|吉林|黑龙江|内蒙古|宁夏|北京|天津|上海|重庆|香港|澳门|台湾)$'
        <!-- continent 兼容 dirty data: 同 selectSearchPage 注释. 当 continent = '亚洲' 时,
             既匹配 country='亚洲' (脏数据) 又匹配 country IN ('中国', '日本', ...) (正经国家). -->
        <if test="continent != null and continent != ''">
            AND (
                u.country = #{continent}
                <if test="continentCountries != null and !continentCountries.isEmpty()">
                    OR u.country IN
                    <foreach collection="continentCountries" item="c" open="(" separator="," close=")">
                        #{c}
                    </foreach>
                </if>
            )
        </if>
        ORDER BY u.country ASC
    </select>

</mapper>
